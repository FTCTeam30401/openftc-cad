# Sensors

Brackets and mounts that position sensors precisely and repeatably.

**Examples:** camera mounts, OTOS mounts, distance/color-sensor brackets, IMU mounts.

Sensor mounts often need adjustability (angle, height) — expose those as parameters rather than shipping fixed variants.
